# Ngrok configuration file for Castor Elecciones backend
# Usage: ngrok http --config=ngrok.yml 80

version: "2"
authtoken: ***REMOVED***

tunnels:
  castor-backend:
    proto: http
    addr: 80
    subdomain: castorelecciones
    inspect: false

